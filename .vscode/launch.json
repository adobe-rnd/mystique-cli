{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: App",
      "type": "debugpy",
      "request": "launch",
      "module": "app.app",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}
